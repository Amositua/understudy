import { useCallback, useEffect, useMemo, useState } from "react";
import type { Message, StateResponse } from "../lib/messages";
import type { Step, Trace } from "../lib/types";
import { ProcedureSchema, type Procedure, type ProcedureInput, type ProcedureStep } from "../lib/procedure";

const actions = ["click", "type", "select", "navigate", "extract", "wait"] as const;
function description(step: Step): string { if (step.type === "navigate") return `Navigated to '${step.page_title || step.url}' (${step.url})`; const name = step.target.accessible_name || step.target.text || step.target.tag; const verbs: Record<Exclude<Step["type"], "navigate">, string> = { click: "Clicked", input: "Typed into", change: "Changed", scroll: "Scrolled to" }; return `${verbs[step.type]} '${name}'`; }
function StepList({ steps }: { steps: Step[] }) { return <ol className="space-y-2">{steps.map((step) => <li key={step.index} className="rounded-lg border border-slate-800 bg-slate-900 px-3 py-2 text-sm"><span className="mr-2 text-xs text-slate-500">{step.index + 1}</span>{description(step)}</li>)}</ol>; }

function Review({ procedure, rawCount, onSave, onBack }: { procedure: Procedure; rawCount: number; onSave: (p: Procedure) => void; onBack: () => void }) {
  const [draft, setDraft] = useState(procedure);
  const update = (patch: Partial<Procedure>) => setDraft((value) => ({ ...value, ...patch }));
  const updateStep = (index: number, patch: Partial<ProcedureStep>) => update({ steps: draft.steps.map((step, i) => i === index ? { ...step, ...patch } : step) });
  const moveStep = (index: number, direction: -1 | 1) => { const destination = index + direction; if (destination < 0 || destination >= draft.steps.length) return; const steps = [...draft.steps]; [steps[index], steps[destination]] = [steps[destination], steps[index]]; update({ steps }); };
  return <section className="space-y-4"><button onClick={onBack} className="text-sm text-indigo-300">← Back</button><input value={draft.name} onChange={(e) => update({ name: e.target.value })} className="w-full bg-transparent text-lg font-semibold outline-none" /><textarea value={draft.description} onChange={(e) => update({ description: e.target.value })} className="w-full resize-none rounded border border-slate-700 bg-slate-900 p-2 text-sm" /><p className="rounded bg-indigo-500/10 p-2 text-sm text-indigo-200">The raw recording had {rawCount} actions; this procedure has {draft.steps.length} steps.</p><h2 className="font-medium">Inputs</h2>{draft.inputs.map((input, index) => <div key={`${input.key}-${index}`} className="flex gap-2"><input value={input.label} onChange={(e) => update({ inputs: draft.inputs.map((item, i) => i === index ? { ...item, label: e.target.value } : item) })} className="min-w-0 flex-1 rounded border border-slate-700 bg-slate-900 p-2 text-sm" /><select value={input.kind} onChange={(e) => update({ inputs: draft.inputs.map((item, i) => i === index ? { ...item, kind: e.target.value as ProcedureInput["kind"] } : item) })} className="rounded bg-slate-800 text-xs">{["text", "secret", "date", "number"].map((kind) => <option key={kind}>{kind}</option>)}</select><button onClick={() => update({ inputs: draft.inputs.filter((_, i) => i !== index) })}>×</button></div>)}<button onClick={() => update({ inputs: [...draft.inputs, { key: `input_${draft.inputs.length + 1}`, label: "New input", kind: "text" }] })} className="text-sm text-indigo-300">+ Add input</button><h2 className="font-medium">Procedure</h2><ol className="space-y-3">{draft.steps.map((step, index) => <li key={step.step_id} className={`rounded-lg border p-3 ${step.action === "extract" ? "border-amber-400/60 bg-amber-400/10" : step.optional ? "border-slate-800 bg-slate-900/50 opacity-70" : "border-slate-700 bg-slate-900"}`}><div className="mb-2 flex gap-2"><span className="text-xs text-slate-500">{index + 1}</span><select value={step.action} onChange={(e) => updateStep(index, { action: e.target.value as ProcedureStep["action"] })} className="rounded bg-slate-800 px-2 text-xs">{actions.map((action) => <option key={action}>{action}</option>)}</select><button onClick={() => moveStep(index, -1)} disabled={index === 0} className="text-xs disabled:opacity-30">↑</button><button onClick={() => moveStep(index, 1)} disabled={index === draft.steps.length - 1} className="text-xs disabled:opacity-30">↓</button><label className="ml-auto text-xs"><input checked={step.optional} type="checkbox" onChange={(e) => updateStep(index, { optional: e.target.checked })} /> optional</label><button onClick={() => update({ steps: draft.steps.filter((_, i) => i !== index) })}>×</button></div><input value={step.intent} onChange={(e) => updateStep(index, { intent: e.target.value })} className="w-full bg-transparent text-base font-medium outline-none" /><input value={step.target_description} onChange={(e) => updateStep(index, { target_description: e.target.value })} className="mt-1 w-full bg-transparent text-sm text-slate-400 outline-none" />{step.extract && <div className="mt-2 text-xs font-medium text-amber-200">Extract → {step.extract.output_key}: {step.extract.what}</div>}<input value={step.expected_page} onChange={(e) => updateStep(index, { expected_page: e.target.value })} className="mt-2 w-full rounded bg-slate-950 p-2 text-xs text-slate-400 outline-none" /></li>)}</ol><button onClick={() => { const result = ProcedureSchema.safeParse(draft); if (result.success) onSave(result.data); }} className="w-full rounded-lg bg-indigo-500 p-3 font-semibold">Save procedure</button></section>;
}

export function App() {
  const [recording, setRecording] = useState(false); const [activeTrace, setActiveTrace] = useState<Trace | null>(null); const [traces, setTraces] = useState<Trace[]>([]); const [procedures, setProcedures] = useState<Procedure[]>([]); const [note, setNote] = useState(""); const [selected, setSelected] = useState<Trace | null>(null); const [review, setReview] = useState<{ procedure: Procedure; rawCount: number } | null>(null); const [settings, setSettings] = useState(false); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [userEmail, setUserEmail] = useState<string>(); const [busy, setBusy] = useState(false); const [error, setError] = useState("");
  const refresh = useCallback(async () => { const r = await chrome.runtime.sendMessage({ type: "GET_STATE" } satisfies Message) as StateResponse; setRecording(r.state.recording); setActiveTrace(r.state.trace); setTraces(r.traces ?? []); setProcedures(r.procedures ?? []); setUserEmail(r.userEmail); }, []);
  useEffect(() => { void refresh(); const listener = (m: Message) => { if (m.type === "GET_STATE") void refresh(); }; chrome.runtime.onMessage.addListener(listener); return () => chrome.runtime.onMessage.removeListener(listener); }, [refresh]);
  const send = async (message: Message): Promise<unknown> => { const response = await chrome.runtime.sendMessage(message) as { error?: unknown }; if (typeof response?.error === "string") throw new Error(response.error); return response; };
  const report = (reason: unknown) => setError(reason instanceof Error ? reason.message : String(reason));
  const compile = async (trace: Trace) => { setBusy(true); setError(""); try { const response = await send({ type: "COMPILE_TRACE", trace } as Message) as { procedure?: Procedure }; if (!response.procedure) throw new Error("The compiler returned no procedure."); setReview({ procedure: response.procedure, rawCount: trace.steps.length }); await refresh(); } catch (e) { report(e); } finally { setBusy(false); } };
  const detail = selected ?? (recording ? activeTrace : null); const screenshotMap = useMemo(() => new Map(detail?.screenshots.map((item) => [item.step_index, item.data_url])), [detail]);
  if (review) return <main className="min-h-screen p-4"><Review procedure={review.procedure} rawCount={review.rawCount} onBack={() => setReview(null)} onSave={async (procedure) => { try { await send({ type: "SAVE_PROCEDURE", procedure }); setReview(null); await refresh(); } catch (e) { report(e); } }} /></main>;
  if (settings) return <main className="min-h-screen p-4 space-y-3"><button onClick={() => setSettings(false)} className="text-sm text-indigo-300">← Back</button><h1 className="text-lg font-semibold">Your Understudy account</h1><p className="text-xs text-slate-400">Create an account or sign in to compile procedures. Your OpenAI key stays on the Understudy server.</p>{error && <p className="rounded bg-red-500/10 p-2 text-sm text-red-200">{error}</p>}{userEmail ? <><p className="text-sm text-emerald-300">Signed in as {userEmail}</p><button onClick={() => void send({ type: "LOGOUT" }).then(refresh).catch(report)} className="w-full rounded bg-slate-700 p-2">Sign out</button></> : <><input value={email} onChange={(e) => setEmail(e.target.value)} placeholder="Email" className="w-full rounded border border-slate-700 bg-slate-900 p-3 text-sm" /><input type="password" value={password} onChange={(e) => setPassword(e.target.value)} placeholder="Password (12+ characters)" className="w-full rounded border border-slate-700 bg-slate-900 p-3 text-sm" /><div className="flex gap-2"><button onClick={() => void send({ type: "AUTH_REGISTER", email, password }).then(refresh).catch(report)} className="flex-1 rounded bg-indigo-500 p-2">Create account</button><button onClick={() => void send({ type: "AUTH_LOGIN", email, password }).then(refresh).catch(report)} className="flex-1 rounded bg-slate-700 p-2">Sign in</button></div></>}</main>;
  return <main className="min-h-screen p-4"><header className="mb-6 flex items-center justify-between"><div><h1 className="text-lg font-semibold">Understudy</h1><p className="text-xs text-slate-400">Record, then compile intent.</p></div><button onClick={() => setSettings(true)} className="text-slate-400">⚙</button></header>{error && <p className="mb-3 rounded bg-red-500/10 p-2 text-sm text-red-200">{error}</p>}{recording ? <section className="space-y-4"><div className="rounded border border-red-500/30 bg-red-500/10 p-3 text-sm">● Recording {activeTrace?.steps.length ?? 0} steps</div><StepList steps={activeTrace?.steps ?? []} /><div className="flex gap-2"><input value={note} onChange={(e) => setNote(e.target.value)} placeholder="Add a note about the last step" className="min-w-0 flex-1 rounded border border-slate-700 bg-slate-900 px-3" /><button onClick={() => { void send({ type: "ADD_NOTE", text: note }); setNote(""); }}>Note</button></div><button onClick={() => void send({ type: "STOP_RECORDING" }).then(refresh)} className="w-full rounded bg-slate-100 p-3 font-semibold text-slate-950">Done</button></section> : detail ? <section className="space-y-3"><button onClick={() => setSelected(null)} className="text-sm text-indigo-300">← All traces</button><StepList steps={detail.steps} /><button disabled={busy} onClick={() => void compile(detail)} className="w-full rounded bg-indigo-500 p-3 font-semibold disabled:opacity-50">{busy ? "Compiling intent…" : "Compile into procedure"}</button>{detail.steps.map((step) => screenshotMap.get(step.index) && <img key={step.index} src={screenshotMap.get(step.index)} className="w-full rounded border border-slate-800" />)}</section> : <section className="space-y-5"><button onClick={() => void send({ type: "START_RECORDING" }).then(refresh)} className="w-full rounded bg-indigo-500 p-3 font-semibold">Teach me</button><div><h2 className="mb-2 text-sm font-medium">Recorded traces</h2>{traces.map((trace) => <button key={trace.trace_id} onClick={() => setSelected(trace)} className="mb-2 w-full rounded border border-slate-800 bg-slate-900 p-3 text-left text-sm">{trace.steps.length} actions <span className="text-slate-500">· {new Date(trace.started_at).toLocaleString()}</span></button>)}</div><div><h2 className="mb-2 text-sm font-medium">Procedures</h2>{procedures.map((procedure) => <button key={procedure.procedure_id} onClick={() => setReview({ procedure, rawCount: procedure.steps.length })} className="mb-2 w-full rounded border border-indigo-500/30 bg-indigo-500/10 p-3 text-left text-sm">{procedure.name}<span className="block text-xs text-slate-400">{procedure.steps.length} durable steps</span></button>)}</div></section>}</main>;
}
